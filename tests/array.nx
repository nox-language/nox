fun main() = (
    var len = 12;
    var a = [0; 12];
    var b = [7; 12];
    var value = a[0];
    printi(value);
    var value = b[0];
    printi(value);
    a[0] = 10;
    printi(a[0]);
    for i = 0 to len - 1 do
        a[i] = i;
    for i = 0 to len - 1 do
        printi(a[i])
)
