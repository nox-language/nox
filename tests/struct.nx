struct Point {
    x: int,
    y: int,
}

fun main() = (
    var point = Point {
        x = 42,
        y = 24,
    };
    printi point.x;
    printi point.y;
    point.y = 100;
    printi point.x;
    printi point.y;
    point.x = 200;
    printi point.x;
    printi point.y;
)
